# Environment Variables for Data Analytics Dashboard
# Copy this file to .env and customize as needed

# Port for the server (optional, defaults to 5000)
PORT=5000

# Open-Meteo Weather API Configuration
# This is a completely free weather API - no API key required!
WEATHER_API_BASE_URL=https://api.open-meteo.com/v1
WEATHER_API_ENDPOINT=/forecast

# Weather API Parameters (optional, can be overridden in code)
WEATHER_DEFAULT_UNITS=metric
WEATHER_TIMEZONE=auto

# Note: Cryptocurrency and GitHub data use free public APIs which don't require API keys
